services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
     - .:/app
     - ./node_modules:/app/node_modules
    environment:
      NODE_ENV: development
      SASS_SILENCE_DEPRECATIONS: legacy-js-api
    stdin_open: true
    tty: true
    command: npm start